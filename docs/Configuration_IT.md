# Configurazione

La configurazione viene salvata in `config.yaml`.

La GUI consente di selezionare i file, controllare i nomi delle colonne, configurare le cartelle di output e scegliere quali fasi della pipeline eseguire.

Modifica manualmente `config.yaml` solo se sai già quale opzione vuoi cambiare.
